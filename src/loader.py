import importlib, os, inspect

from os import listdir
def recursive_filesearch(filename):

    for dir in os.listdir():

        if os.Path.isdir():
            return recursive_filesearch(filename)
        else:
            if 


for name, cls in inspect.getmembers(importlib.import)